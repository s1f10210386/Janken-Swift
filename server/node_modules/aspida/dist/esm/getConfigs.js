var _a;
import fs from 'fs';
import path from 'path';
const defaultConfig = {
    input: (_a = ['api', 'apis'].find(input => fs.existsSync(input))) !== null && _a !== void 0 ? _a : 'api',
    baseURL: '',
    trailingSlash: false,
    outputEachDir: false,
    outputMode: 'all',
};
export const getConfigs = (config = 'aspida.config.js') => {
    const configs = typeof config !== 'string'
        ? config
        : fs.existsSync(config)
            ? require(path.join(process.cwd(), config))
            : defaultConfig;
    return Array.isArray(configs)
        ? configs.map(c => ({ ...defaultConfig, ...c }))
        : [{ ...defaultConfig, ...configs }];
};
//# sourceMappingURL=getConfigs.js.map